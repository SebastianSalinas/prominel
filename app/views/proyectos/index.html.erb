<p id="notice"><%= notice %></p>

<h1>Listing Proyectos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Presupuesto</th>
      <th>Obrero</th>
      <th>Lat ubicacion</th>
      <th>Lon ubicacion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @proyectos.each do |proyecto| %>
      <tr>
        <td><%= proyecto.nombre %></td>
        <td><%= proyecto.presupuesto %></td>
        <td><%= proyecto.obrero %></td>
        <td><%= proyecto.lat_ubicacion %></td>
        <td><%= proyecto.lon_ubicacion %></td>
        <td><%= link_to 'Show', proyecto %></td>
        <td><%= link_to 'Edit', edit_proyecto_path(proyecto) %></td>
        <td><%= link_to 'Destroy', proyecto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Proyecto', new_proyecto_path %>
